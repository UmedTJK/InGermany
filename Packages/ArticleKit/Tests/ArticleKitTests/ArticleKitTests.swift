
/*
import XCTest
@testable import ArticleKit

final class ArticleKitTests: XCTestCase {
    func testExample() throws {
        // пример теста
        let block = ArticleBlock(id: UUID(), type: .paragraph, payload: .text("Hello"))
        XCTAssertEqual(block.type, .paragraph)
    }
}
*/
